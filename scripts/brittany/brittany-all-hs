set +e

for f in $(find . -not -path "*/.stack-work*/*" -not -path "*specs/*" -name '*.hs' | xargs grep -L QuasiQuotes); do
    brittany --config-file scripts/brittany/config.yaml  --write-mode inplace $f &
done

wait